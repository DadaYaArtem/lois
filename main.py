# ============================================================
# Главный модуль: точка входа, меню программы
#
# Лабораторная работа №1 по ЛОИС
# Тема: Построение СДНФ формулы языка логики высказываний
#
# Вариант 9: Построить СДНФ для заданной формулы
#            сокращённого языка логики высказываний
#
# Формат ввода:
#   /\  — конъюнкция (И)
#   \/  — дизъюнкция (ИЛИ)
#   !   — отрицание (НЕ)
#   ->  — импликация
#   ~   — эквиваленция
#   A-Z — пропозициональные переменные
# ============================================================

from solve_mode import solve_mode
from test_mode import test_mode


def main():
    print("=" * 50)
    print("  Построение СДНФ формулы логики высказываний")
    print("  Лабораторная работа №1 по ЛОИС")
    print("=" * 50)

    while True:
        print("\nВыберите режим работы:")
        print("  1) Построить СДНФ для формулы")
        print("  2) Тестирование знаний")
        print("  3) Выход")

        try:
            choice = input("\nВаш выбор (1/2/3) >>> ").strip()
        except EOFError:
            break

        if choice == "1":
            solve_mode()
        elif choice == "2":
            test_mode()
        elif choice == "3":
            print("\nДо свидания!")
            break
        else:
            print("Неизвестный выбор. Попробуйте ещё раз.")


if __name__ == "__main__":
    main()
